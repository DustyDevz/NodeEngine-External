# NodeEngine - External
This repository holds all the external DLLs, libs, and includes required to build and run the project.

## Build Requirement
Navigate to `bin/ffmpeg` and extract `avcodec-62.rar`. This is required as github no like 100mb :(
